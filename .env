# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL Database (Used by: postgres, fastapi, setup, pgadmin)
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=nedi
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ClickHouse Database (Used by: clickhouse, dbt, setup, ch-ui)
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=yourpassword
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=9000

# ============================================================================
# DATABASE UI CONFIGURATION
# ============================================================================

# pgAdmin (Used by: pgadmin)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin123

# CH-UI (ClickHouse UI) (Used by: ch-ui)
VITE_CLICKHOUSE_URL=http://localhost:8123
VITE_CLICKHOUSE_USER=default
VITE_CLICKHOUSE_PASS=yourpassword
VITE_CLICKHOUSE_USE_ADVANCED=false
VITE_CLICKHOUSE_REQUEST_TIMEOUT=30000

# ============================================================================
# DAGSTER CONFIGURATION
# ============================================================================

# Dagster PostgreSQL (Used by: dagster_postgresql, dagster_webserver, dagster_daemon, dagster_user_code)
DAGSTER_POSTGRES_USER=dagster_user
DAGSTER_POSTGRES_PASSWORD=dagster_password
DAGSTER_POSTGRES_DB=dagster_db
DAGSTER_CURRENT_IMAGE=dagster_user_code_image

# ClickHouse HTTP Interface (Used by: setup, dbt, dagster_user_code)
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_HTTP_URL=http://clickhouse:8123

# ============================================================================
# AIRBYTE CONFIGURATION
# ============================================================================

# Airbyte API Configuration (Used by: dagster_user_code)
AIRBYTE_BASE_URL=https://unappendaged-unseized-mable.ngrok-free.dev/api/v1
AIRBYTE_CONNECTION_ID=2c403088-b6e5-4a25-8700-08491ea106df
AIRBYTE_CLIENT_ID=4910a79b-c5df-4f7b-977b-d5f310d04746
AIRBYTE_CLIENT_SECRET=b7LeM52Tzsb9DIt6rTluP64hAJYQ1Qvw